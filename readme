TODO:
add time picker to form
display start time when auction created
maxBid
ticker isn't working
start redirecting on create again
//select control for type of auctions?
change from createdAt, to user set date, for start of auction
the bid behaviour for ordinary item is wrong
end start auction cron job

low:
remove remove all methods, and the buttons
stop publishing everything

array for possible bids can be generated by take base value, adding modifier that is squared by like 1.2 every so often.


Note:

user usernames are not hashed, Random.id just generates random string for username

